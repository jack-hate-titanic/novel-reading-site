@AGENTS.md

# Claude Code 项目指令补充

## 协作语言

- 解释、摘要、需求确认优先使用简体中文
- 代码、路由名、命令名保持英文
- UI 文案保持英文，除非用户明确要求中文

## 验证流程

每次有意义的改动后，按需运行：

1. `npm run test:run`
2. `npm run lint`
3. `npm run build`
4. 视觉/交互改动：`npm run dev` 并验证受影响路由

## 回复格式

每次回复末尾包含：

1. 改动摘要
2. 涉及的文件路径
3. 已运行的验证命令及结果
4. 未验证的内容需明确说明

